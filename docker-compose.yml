services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: veggtable-sqlserver
    platform: linux/amd64
    environment:
      ACCEPT_EULA: "Y"
      # Local-dev default, not a real secret. Override with a .env file if you want.
      MSSQL_SA_PASSWORD: "${MSSQL_SA_PASSWORD:-Veggtable#7}"
      MSSQL_PID: "Developer"
    ports:
      # localhost only, host port 14333 avoids a local SQL Server on 1433.
      - "127.0.0.1:14333:1433"
    volumes:
      - veggtable-mssql-data:/var/opt/mssql
    healthcheck:
      test:
        - "CMD-SHELL"
        - "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P \"$$MSSQL_SA_PASSWORD\" -C -Q \"SELECT 1\" || exit 1"
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  api:
    build:
      # Build context is repo root so Dockerfile can COPY src/...
      context: .
      dockerfile: src/VEGG.TABLE.API/Dockerfile
    container_name: veggtable-api
    ports:
      - "127.0.0.1:5167:5167"
      - "127.0.0.1:7277:7277"
    environment:
      # API talks to sqlserver via the container hostname, not localhost:14333
      ConnectionStrings__DefaultConnection: "Server=sqlserver,1433;Database=master;User Id=sa;Password=Veggtable#7;TrustServerCertificate=true;"
      Jwt__Key: "YourSuperSecretKeyMustBeAtLeast16CharactersLong"
      Jwt__Issuer: "VEGG.TABLE.API"
      Jwt__Audience: "VEGG.TABLE.Client"
      ASPNETCORE_URLS: "https://+:7277;http://+:5167"
      ASPNETCORE_Kestrel__Certificates__Default__Path: /https/aspnetapp.pfx
      ASPNETCORE_Kestrel__Certificates__Default__Password: yourpassword123
    volumes:
      - ${HOME}/.aspnet/https:/https:ro
    depends_on:
      sqlserver:
        condition: service_healthy

  client:
    build:
      context: .
      dockerfile: src/VEGG.TABLE.Client/Dockerfile
    container_name: veggtable-client
  
  caddy:
    image: caddy:latest
    container_name: veggtable-proxy
    ports:
      - "5209:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
    depends_on:
      - client
      - api


volumes:
  veggtable-mssql-data:
  caddy_data:
